# Building

Optional Step to clear the npm cache because we've been fighting lately:
~~~ bash
~~~

~~~ bash
npm install # To bring packages into node_modules
npm run build # To Actually build this package into dist/
~~~

# Serving Dev Server

~~~ bash
npm run dev
~~~